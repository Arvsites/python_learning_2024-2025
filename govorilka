# Программа для ввода 10 вопросов и получения ответов
def ask_questions():
    # Словарь с вопросами и ответами (можно расширить или подключить API)
    qa_pairs = {
        "какая сегодня погода?": "Сегодня солнечно, +20°C.",
        "сколько времени?": "Много.",
        "что такое python?": "Python — это высокоуровневый язык программирования.",
        "как написать цикл?": "Пример цикла: for i in range(5): print(i)",
        "что такое ai?": "ИИ — это искусственный интеллект, имитирующий человеческие способности.",
        "какой сейчас год?": "Сегодня 2025 год.",
        "как приготовить борщ?": "Нужны свекла, капуста, мясо, картофель. Варить 2 часа.",
        "что такое ооп?": "ООП — это объектно-ориентированное программирование.",
        "как работает интернет?": "Интернет — сеть компьютеров, передающих данные по протоколам.",
        "кто такой илон маск?": "Илон Маск — предприниматель, основатель Tesla и SpaceX."
    }
    
    print("Задайте вопрос. ")
    
    # Список для хранения вопросов и ответов
    results = []
    
    # Цикл для ввода 10 вопросов
    for i in range(10):
        question = input(f"Вопрос {i+1}: ").strip().lower()
        # Проверяем, есть ли вопрос в базе, если нет — даем общий ответ
        answer = qa_pairs.get(question, "Извините, я не знаю ответа на этот вопрос.")
        results.append((question, answer))
    
    # Выводим все вопросы и ответы
    print("\nВаши вопросы и ответы:")
    for i, (question, answer) in enumerate(results, 1):
        print(f"Вопрос {i}: {question}")
        print(f"Ответ: {answer}\n")

# Запускаем программу
if __name__ == "__main__":
    ask_questions()
